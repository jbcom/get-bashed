# secrets.d

Place local, non-versioned secret snippets here. Files ending in `.sh` are sourced by
`bashrc.d/99-secrets.sh` in lexicographic order.

Example:
```
~/.get-bashed/secrets.d/00-local.sh
```
